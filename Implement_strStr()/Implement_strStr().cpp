class Solution {
public:
    int strStr(string haystack, string needle) {
        int index;
        index = haystack.find(needle, 0);
        if(index >= 0) return index;
        else return index;
    }
};
